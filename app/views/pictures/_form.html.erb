<!--- ADD PHOTOS FORM -->

<%= form_for(picture) do |f| %>
  <% if picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    


      <div class="input-group">
        <%= f.text_field :url,  class: "form-control", "aria-describedby"=> "url-addon", placeholder: "Img URL" %>
        <span class="input-group-btn" id="url-addon">
          <%= f.submit "Analyze", class: "btn btn-success" %>
        </span>
      </div>
      <div class="input-group">
      <label class="btn btn-primary btn-file">
        <i class="fa fa-camera"></i> <input type="file" accept="image/*" capture="camera" aria-describedby="file" style="display:none;">
      </label>
      </div>



        

<% end %>
